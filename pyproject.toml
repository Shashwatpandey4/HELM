[project]
name = "helm"
version = "0.1.0"
description = "Heterogeneous Execution for Large Models"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.0.0",
    "psutil",
    "numpy",
    "packaging"
]

[tool.uv]
package = true
