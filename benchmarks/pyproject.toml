[project]
name = "project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "torch>=2.9.1",
    "transformers>=4.57.3",
    "accelerate",
    "psutil",
    "nvidia-ml-py",
    "matplotlib",
]
